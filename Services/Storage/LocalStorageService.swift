import Foundation

class LocalStorageService {
    // Handles all interactions with SwiftData/CoreData to ensure 
    // no data ever leaves the device.
    func saveJob(_ job: Job) {
        // SwiftData logic
    }
    
    func saveResume(_ resume: Resume) {
        // SwiftData logic
    }
}

